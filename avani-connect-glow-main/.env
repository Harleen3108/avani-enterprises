# Development Environment Variables
# These are used when running locally (npm run dev)

# Local backend URL for development
VITE_BACKEND_URL=http://localhost:5000
VITE_API_URL=http://localhost:5000

# NOTE: For production deployment, the app will automatically use:
# https://avani-enterprises-backend.vercel.app
# You can override this by setting VITE_API_URL in your deployment platform's environment variables
